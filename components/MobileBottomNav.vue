<template>
  <nav class="bottom-nav" aria-label="Brze akcije">
    <a :href="shop.telLink" class="bn-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.91.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92z" />
      </svg>
      <span>{{ t.mobileNav.contact }}</span>
    </a>

    <a href="#lokacija" class="bn-item">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" />
        <circle cx="12" cy="10" r="3" />
      </svg>
      <span>{{ t.mobileNav.location }}</span>
    </a>

    <a href="#termin" class="bn-item bn-cta">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="3" y="4" width="18" height="18" rx="2" />
        <line x1="16" y1="2" x2="16" y2="6" />
        <line x1="8" y1="2" x2="8" y2="6" />
        <line x1="3" y1="10" x2="21" y2="10" />
      </svg>
      <span>{{ t.mobileNav.book }}</span>
    </a>

    <button class="bn-item" @click="openAgent">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
      </svg>
      <span>{{ t.mobileNav.ask }}</span>
    </button>
  </nav>
</template>

<script setup>
const shop = useShopInfo()
const t = useT()

const openAgent = () => {
  window.dispatchEvent(new CustomEvent('open-agent'))
}
</script>

<style scoped>
.bottom-nav {
  display: none;
}

@media (max-width: 760px) {
  .bottom-nav {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 95;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    background: rgba(15, 15, 17, 0.96);
    backdrop-filter: blur(16px);
    border-top: 1px solid var(--line);
    padding: 8px 6px calc(8px + env(safe-area-inset-bottom, 0px));
  }
  .bn-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    background: transparent;
    border: none;
    cursor: pointer;
    color: var(--text-2);
    font-family: var(--font-cond);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.68rem;
    padding: 6px 2px;
    transition: color 0.2s ease;
  }
  .bn-item svg {
    width: 22px;
    height: 22px;
  }
  .bn-item:active {
    color: var(--orange);
  }
  .bn-cta {
    color: var(--orange);
  }
  .bn-cta svg {
    filter: drop-shadow(0 0 8px rgba(255, 90, 31, 0.5));
  }
}
</style>
